<odoo>
    <template id="report_vehicle_feature_basic_document">
        <t t-call="web.html_container">
            <div t-if="not o" t-set="o" t-value="doc" />
            <div
                class="article o_table_standard"
                t-att-data-oe-model="o and o._name"
                t-att-data-oe-id="o and o.id"
                t-att-data-oe-lang="o and o.env.context.get('lang')"
            >
                <div class="page">
                    <h3>Características de vehículo</h3>
                    <table class="table table-sm">
                        <thead>
                            <tr>
                                <th>Categoría</th>
                                <th>Característica</th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="docs" t-as="rec">
                                <tr>
                                    <td>
                                        <span
                                            t-field="rec.vehicle_feature_category_id"
                                        />
                                    </td>
                                    <td>
                                        <span t-field="rec.name" />
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="footer">
                <div t-if="report_type == 'pdf'" class="text-end text-muted">
                    Página <span class="page" /> / <span class="topage" />
                </div>
            </div>
        </t>
    </template>
</odoo>
